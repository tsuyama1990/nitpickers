1. **Create `src/services/environment_validator.py`**:
   - Extract `verify_environment_and_observability`, `_verify_observability`, `_verify_required_keys`, `_scan_implicit_dependencies`, and `_verify_dynamic_requirements` from `src/services/workflow.py` into a new `EnvironmentValidator` class.
   - Update imports in `src/services/environment_validator.py` as needed.
2. **Update `src/services/async_dispatcher.py`**:
   - Add a new method `execute_batches(self, batches: list[list[CycleManifest]], coroutine_factory: Callable[[CycleManifest], Coroutine[Any, Any, Any]]) -> None` to `AsyncDispatcher`.
   - The method will loop through `batches`, tracking staggered execution with `asyncio.sleep(0.5)` for parallel items, and execute `asyncio.gather(*tasks, return_exceptions=False)` for each batch to maintain fail-fast behavior.
3. **Update `src/services/workflow.py`**:
   - Remove the `verify_environment_and_observability` method and its sub-methods.
   - Import `EnvironmentValidator` and replace `self.verify_environment_and_observability()` with `EnvironmentValidator().verify_environment_and_observability()`.
   - In `_run_all_cycles`, replace the `for i, batch in enumerate(batches): ... asyncio.gather(*tasks)` logic with a call to `dispatcher.execute_batches(batches, cycle_runner_callback)`. Here, `cycle_runner_callback` is a wrapper around `self._run_single_cycle(...)`.
   - In `_run_parallel_coder_phase`, similarly replace the batched execution logic with a call to `dispatcher.execute_batches(batches, cycle_runner_callback)`.
4. **Pre-commit**:
   - Ensure proper testing, verification, review, and reflection are done.
5. **Test**:
   - Run linter/typing (`uv run ruff check src`, `uv run mypy src`).
   - Run tests (`uv run pytest tests`).
