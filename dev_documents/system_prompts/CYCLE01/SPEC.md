# CYCLE01 Specification: Comprehensive 5-Phase Architecture Refactoring

## Summary
The singular development cycle, CYCLE01, is meticulously engineered to comprehensively orchestrate the absolute entirety of the monumental 5-phase architectural refactoring required to transform the NITPICKERS intelligent agent workflow. This monolithic, all-encompassing cycle is fundamentally designed to systematically overhaul the foundational Pydantic state models, fundamentally rewire the exceedingly complex LangGraph execution routing logic, establish the revolutionary new Integration Phase featuring advanced 3-Way Diff conflict resolution, and meticulously separate the ultimate User Acceptance Testing (UAT) sequence into its own dedicated, post-integration execution graph. The overarching, non-negotiable objective is to instantaneously manifest a highly deterministic, inherently zero-trust operational pipeline. Within this highly fortified pipeline, every single synthesized code modification is ruthlessly subjected to a grueling sequence: localized structural sandboxing, a rigorous and unyielding serial auditing process conducted by entirely independent diagnostic agents, a specialized and dedicated refactoring loop strictly focused on elevating code quality, an incredibly sophisticated intelligent conflict resolution protocol functioning flawlessly during parallel integration, and finally, a globally executed End-to-End dynamic testing validation phase. By systematically encapsulating this profoundly complex systemic evolution into a single, cohesive developmental sprint, we unequivocally guarantee that all interdependent components—ranging from the intricate `CycleState` property trackers to the overarching, high-level workflow orchestrator—are seamlessly synchronized, mathematically verified, and permanently established. This foundational work definitively ensures that the resulting software synthesis environment is unimaginably stable, exceptionally scalable, and completely resilient to the catastrophic cascading failures frequently observed in less rigorously designed, loosely coupled agentic systems. This singular cycle constitutes the complete technological bedrock of the entire modernized architecture.

## Infrastructure & Dependencies

### A. Project Secrets (`.env.example`)
- The Coder must explicitly append the following highly critical external service variables directly to the `.env.example` file.
- These variables unequivocally represent the fundamental required external services discovered in the explicit architectural specifications for this comprehensive cycle.
- **Instruct the Coder to append these with clear `# Target Project Secrets` comments:**
  ```env
  # Target Project Secrets
  OPENROUTER_API_KEY=your_openrouter_api_key_here
  JULES_API_KEY=your_jules_api_key_here
  E2B_API_KEY=your_e2b_api_key_here
  ```

### B. System Configurations (`docker-compose.yml`)
- Non-confidential environmental setups required for the robust operation of the refactored 5-phase architecture must be explicitly defined.
- **Instruct the Coder to place these directly into the `environment:` section of the relevant service in `docker-compose.yml`:**
  - `NITPICK_NUM_AUDITORS=3` (Explicitly defining the exact number of serial diagnostic agents).
  - `NITPICK_MAX_ITERATIONS=2` (Strictly enforcing the maximum allowable rejection loops before triggering a systemic circuit breaker).
- **Explicit Instruction:** The Coder is unequivocally mandated to preserve absolutely all valid YAML formatting and strictly maintain idempotency. Under no circumstances should the Coder overwrite or inadvertently delete any existing agent configurations or network definitions.

### C. Sandbox Resilience (CRITICAL TEST STRATEGY)
- **Mandate Mocking:** You MUST explicitly instruct the Coder that *all external API calls relying on the newly defined secrets in `.env.example` MUST be completely and unequivocally mocked in all unit and integration tests (utilizing `unittest.mock`, `pytest-mock`, or robust frameworks like `respx`)*.
- *Why:* The isolated execution Sandbox fundamentally will not possess the genuine, highly sensitive API keys during the autonomous evaluation phase orchestrated within CI environments. If any integration tests attempt to execute real, live network calls to external SaaS providers without valid, active `.env` values, the testing pipeline will instantly and catastrophically fail, resulting in an unrecoverable, infinite retry loop that entirely blocks the CI/CD deployment process. Absolute isolation must be maintained.

## System Architecture
This incredibly comprehensive section formally delineates the exact, unyielding code blueprints fundamentally required to physically manifest the highly sophisticated 5-Phase Architecture. This monumental structural overhaul mandates precise, surgical modifications across multiple, highly critical domain files, meticulously transitioning the system from a potentially brittle, parallel-committee structure into a rigorously controlled, deterministically serialized, and intelligently integrated multi-agent state machine. The architectural integrity of this precise configuration is absolutely paramount, and the prescribed File Structure explicitly identifies the precise files that are mandated for creation or profound modification throughout the execution of this defining cycle. This blueprint constitutes the absolute, immutable source of truth for the entire refactoring operation.

The fundamental architectural strategy revolves around four primary pillars of systemic evolution. Pillar One mandates the drastic enhancement of the central State Management core (`src/state.py`), injecting sophisticated tracker variables to perfectly monitor the complex serial audit progression and flawlessly dictate the precise transition into the dedicated refactoring phase. Pillar Two necessitates the complete rewiring of the Coder Graph (`src/graph.py` and `src/nodes/routers.py`), aggressively replacing the antiquated, parallel execution logic with a highly fortified, sequential Auditor node, immediately followed by a newly designed Refactor node and a conclusive Final Critic node. Pillar Three introduces the revolutionary Integration Engine (`src/services/conflict_manager.py`), an entirely unprecedented subsystem explicitly engineered to execute advanced 3-Way Diff structural analysis and seamlessly resolve complex code collisions originating from concurrently executing branches. Finally, Pillar Four formalizes the overarching Orchestration logic (`src/services/workflow.py`), brilliantly separating the comprehensive User Acceptance Testing sequence into its own entirely distinct, post-integration Graph execution.

The architectural dependencies and explicit interactions between these sophisticated pillars are profoundly intricate. The fundamental Pydantic state changes implemented within Pillar One provide the indispensable, structured data payloads fundamentally required by the advanced routing logic engineered within Pillar Two. Successfully completing the heavily fortified Phase 2 Coder Graph constitutes an absolute, non-negotiable prerequisite before the Orchestrator (Pillar Four) is officially authorized to invoke the advanced Phase 3 Integration mechanisms (Pillar Three). Ultimately, only after Pillar Three has successfully merged all concurrent changes and secured a flawless global sandbox evaluation can Pillar Four trigger the final Phase 4 E2E evaluation sequence. This strictly enforced, cascading structural dependency mathematically guarantees absolute data integrity, utterly prevents dangerous context bleeding between distinct execution phases, and definitively establishes a universally impenetrable, zero-trust perimeter surrounding all synthesized source code.

**File Structure Blueprint:**
```text
src/
├── cli.py
├── **state.py** (Modifying CycleState and CommitteeState)
├── **graph.py** (Rewiring Coder Graph, creating Integration & QA Graphs)
├── nodes/
│   └── **routers.py** (Implementing new routing logic)
└── services/
    ├── **conflict_manager.py** (Implementing 3-Way Diff logic)
    ├── **uat_usecase.py** (Decoupling for Phase 4 execution)
    └── **workflow.py** (Orchestrating the 5-phase pipeline)
```

The precise, highly intricate interactions formally dictated by this immutable architectural blueprint are detailed as follows. Within `src/state.py`, the foundational `CycleState` model, alongside its highly critical sub-model `CommitteeState`, must be aggressively augmented. These models are the absolute heart of the system, and they must now explicitly track the highly sensitive boolean flag `is_refactoring`, meticulously increment the integer `current_auditor_index`, and aggressively monitor the circuit-breaking integer `audit_attempt_count`. Furthermore, a completely new, rigidly structured `IntegrationState` model must be formally established to specifically handle the incredibly complex queue of validated feature branches (`branches_to_merge`), formally documented conflict lists, and the real-time status of the primary `integration_branch`. These state variables are not merely informative; they constitute the absolute, non-negotiable command signals fundamentally dictating the operational trajectory of the entire LangGraph engine.

Simultaneously, within `src/nodes/routers.py`, entirely new, highly sophisticated conditional routing functions must be precisely engineered. These critical functions—namely `route_sandbox_evaluate`, `route_auditor`, and `route_final_critic`—must be meticulously designed to actively inspect the deeply embedded Pydantic state payloads. Based exclusively on these rigidly typed structural values, they will formulate absolute, deterministic routing decisions, completely independent of any external, unstructured influence. For instance, the `route_auditor` function must intelligently evaluate the audit status, increment the internal attempt counter upon any detected rejection, correctly route the payload back to the Coder agent for mandatory remediation, and intelligently transition the execution flow to the subsequent Auditor only upon receiving an explicit, mathematically verifiable approval signal.

The physical construction of the LangGraph execution flows within `src/graph.py` represents a monumental restructuring effort. The highly complex `_create_coder_graph` method must be fundamentally rewritten, aggressively stripping out the legacy, parallel committee structures. It must be meticulously replaced with a rigidly linear sequence specifically governing the `coder_session`, seamlessly connecting it to the rigorously controlled `sandbox_evaluate` function, subsequently routing the payload through the sequentially executing `auditor_node`, transitioning into the dedicated `refactor_node`, and finally culminating in the critically important `final_critic_node`. Furthermore, an entirely unprecedented Phase 3 mechanism, the `_create_integration_graph`, must be formally constructed. This newly minted graph must orchestrate the highly complex `git_merge_node`, the analytically advanced `master_integrator_node`, and the globally enforced `global_sandbox_node`. Finally, the `_create_qa_graph` must be definitively established as an entirely independent, post-integration execution sequence, fundamentally dedicated exclusively to executing the rigorous, multi-modal Playwright E2E scenarios explicitly defined within the UAT specifications.

The execution of the revolutionary Phase 3 integration logic demands the implementation of exceedingly sophisticated algorithmic procedures within the deeply secured `src/services/conflict_manager.py` module. The highly critical `build_conflict_package` method must be meticulously engineered to actively interface with the underlying Git version control subsystem. It must seamlessly and reliably extract three entirely distinct text representations of any structurally conflicted file: the shared Common Ancestor (Base), the actively modified Local version (Branch A), and the concurrently modified Remote version (Branch B). These three distinct representations must be meticulously fused into a single, flawlessly structured LLM execution prompt explicitly utilizing a strongly typed Pydantic output schema, completely eliminating any reliance on brittle markdown extraction.

Finally, the overarching execution logic permanently residing within the `src/services/workflow.py` Orchestrator module must be entirely refactored to seamlessly manage this incredibly complex, multi-tiered symphony of concurrent processes. The Orchestrator is fundamentally responsible for dynamically launching the massively parallel execution of all authorized Phase 2 Coder cycles, efficiently utilizing `asyncio.gather` or an equivalently robust construct. Critically, it must unconditionally await the absolute, unanimous completion of every single parallel cycle before initiating the highly sensitive Phase 3 Integration procedure. Subsequently, only upon the absolutely confirmed, flawless execution of the Phase 3 `global_sandbox_node` is the Orchestrator officially authorized to deploy the ultimate Phase 4 UAT evaluation sequence, definitively ensuring that only structurally sound, beautifully integrated, and comprehensively tested software ever reaches the final, terminal `END` state.

## Design Architecture
This profoundly advanced software development system is entirely, uncompromisingly designed upon a flawless, schema-first, inherently strongly typed Pydantic foundation. This non-negotiable architectural paradigm mathematically ensures that absolutely every single data transition occurring within the complex LangGraph execution workflow is inherently type-safe, meticulously validated, and structurally sound. This specific section serves as the definitive, pre-implementation design document formally establishing the immutable rules governing the incredibly robust Pydantic-based schemas fundamentally required to empower the 5-phase architecture.

The core domain concepts formally represented within the precisely modified files fundamentally revolve around the absolute, overarching imperative to securely manage, flawlessly track, and seamlessly transport complex execution state across multiple, entirely disparate agentic operational boundaries.

The most critical entity within this system is the `CycleState` model. This monumental data structure represents the absolute entirety of a single, highly specific development cycle (e.g., Phase 2). It is fundamentally mandated to comprehensively encapsulate absolutely all necessary contextual data fundamentally required by the Coder, Sandbox, and Auditor agents. Crucially, it must be significantly augmented to include the deeply integrated `CommitteeState`. This critical sub-model logically groups all variables explicitly tracking the grueling progress of the multi-agent review procedures.

The `is_refactoring` Boolean flag constitutes an absolutely critical, immutable system invariant. When this precise value registers as `False`, the execution engine is strictly operating within the preliminary implementation phase, singularly focused on successfully passing foundational unit tests and basic structural linting checks. Conversely, when this exact value registers as `True`, the system officially acknowledges that the synthesized code has successfully survived the grueling auditor reviews and is now intensely, exclusively focused on elevating internal code quality—enhancing readability, structurally enforcing specific design patterns, and optimizing algorithmic flow—without explicitly altering the underlying, previously validated business logic.

The `current_auditor_index` is a strictly typed integer fundamentally representing the exact, active auditor within the sequentially executing chain. It is bound by an incredibly strict constraint: it must eternally remain $\geq 1$ and simultaneously $\leq$ the total, globally configured number of defined auditors (e.g., dynamically retrieved from `NITPICK_NUM_AUDITORS`). It is explicitly permitted to increment solely upon the reception of a verified, mathematically sound `Approve` action originating directly from the Auditor agent.

The `audit_attempt_count` operates as a highly sensitive integer counter, meticulously tracking the exact, precise number of times any single, specific auditor has aggressively rejected the proposed code. This incredibly vital counter functions as an essential, high-priority circuit breaker, utterly preventing catastrophic, infinite execution loops by strictly enforcing a hard, mathematically defined maximum number of retries (e.g., defined by `NITPICK_MAX_ITERATIONS`) prior to officially forcing a broader, systemic failure or aggressively demanding manual human intervention. This counter must be systematically reset to 0 precisely when the execution flow successfully transitions to a new auditor or immediately following a successful refactoring pass.

To unequivocally guarantee perfect backward compatibility with any pre-existing legacy code that might inappropriately interact directly with the overarching `CycleState`, the newly established tracking variables (`is_refactoring`, `current_auditor_index`, etc.) must be intelligently exposed as sophisticated top-level properties residing directly on the `CycleState` model itself. These highly specialized properties will internally, seamlessly map to the exact underlying fields securely nested within the deeply embedded `CommitteeState` sub-model. This established architectural pattern, already formally present within `src/state.py` utilizing the powerful `@property` decorators, brilliantly ensures that retrieving or modifying these top-level properties instantly and flawlessly updates the securely nested execution state.

Furthermore, absolutely all newly defined models, including any explicit extensions applied directly to the foundational `BaseNode`, are unequivocally mandated to be explicitly configured utilizing `ConfigDict(extra='forbid', strict=True, arbitrary_types_allowed=True, frozen=True)`. This incredibly strict, completely unforgiving configuration mathematically forbids the introduction of any extraneous data fields, utterly preventing silent typographical errors from catastrophically disrupting the incredibly precise LangGraph state machine. It definitively establishes a profoundly secure, completely predictable execution environment fundamentally capable of handling incredibly complex, multi-agent workflows without ever succumbing to unpredictable data drift or catastrophic state corruption.

## Implementation Approach
The highly complex implementation of this massive architectural refactoring must meticulously follow a strict, highly sequential, step-by-step operational process. This fundamental approach aggressively prioritizes impenetrable schema validation and flawless data integrity over mere algorithmic execution.

1.  **State Schema Overhaul (`src/state.py`)**:
    - Aggressively locate the critical `CommitteeState` Pydantic model.
    - Explicitly inject the boolean tracker: `is_refactoring: bool = Field(default=False)`.
    - Explicitly inject the critical circuit breaker: `audit_attempt_count: int = Field(default=0, ge=0)`.
    - Ensure `current_auditor_index` is perfectly defined and flawlessly operational.
    - Intelligently update the overarching `CycleState` property decorators to effortlessly provide getter/setter access directly to these newly embedded fields.
    - Formally define the sophisticated `IntegrationState` model, rigorously incorporating `branches_to_merge: list[str]`, conflict tracking queues, and the active `integration_branch` status.
    - Execute `uv run mypy src/state.py` to mathematically guarantee absolute type safety.

2.  **Routing Logic Engineering (`src/nodes/routers.py`)**:
    - Engineer `route_sandbox_evaluate(state: CycleState) -> str`: Flawlessly evaluate the current `sandbox_status`. If marked "failed", explicitly return `"failed"`. If validated as successful and `is_refactoring` is precisely `True`, securely route to `"final_critic"`. Otherwise, firmly route to `"auditor"`.
    - Engineer `route_auditor(state: CycleState) -> str`: Meticulously evaluate the precise diagnostic result. If definitively `"Reject"`, mathematically increment the `audit_attempt_count`. If this vital counter dangerously exceeds `NITPICK_MAX_ITERATIONS`, brutally trigger a catastrophic failure route. Otherwise, calmly route back to `"coder_session"` for essential remediation. If definitively `"Approve"`, mathematically increment `current_auditor_index`. If this index safely exceeds `NITPICK_NUM_AUDITORS`, triumphantly route to `"pass_all"`. Otherwise, systematically transition to `"next_auditor"`.
    - Engineer `route_final_critic(state: CycleState) -> str`: Simply evaluate the conclusive self-critic output, deterministically routing to either `"reject"` or `"approve"`.

3.  **Graph Engine Rewiring (`src/graph.py`)**:
    - Aggressively obliterate the antiquated, parallel `committee_manager` logic firmly embedded within `_create_coder_graph`.
    - Meticulously instantiate and seamlessly interconnect the powerful new nodes: `auditor_node`, `refactor_node`, and `final_critic_node`.
    - Precisely define the highly complex conditional edges strictly utilizing the previously engineered routing functions. Mathematically ensure the execution flow flawlessly mirrors the precise sequence: Coder $\rightarrow$ Sandbox $\rightarrow$ Serial Auditors $\rightarrow$ Refactor $\rightarrow$ Sandbox $\rightarrow$ Final Critic.
    - Formally construct the entirely unprecedented `_create_integration_graph`, systematically linking the `git_merge_node` to the complex `master_integrator_node` (via conflict conditional routing) and ultimately concluding at the `global_sandbox_node`.
    - Cleanly isolate the ultimate `_create_qa_graph`, ensuring it functions as an entirely discrete, post-integration validation sequence entirely focused on multi-modal E2E Playwright analysis.

4.  **Conflict Resolution & Orchestration (`src/services/`)**:
    - Deeply within `conflict_manager.py`, meticulously engineer the highly complex `build_conflict_package` operational method. Utilize highly robust, asynchronous `ProcessRunner` Git commands to securely fetch the critical Base, Local, and Remote file data structures. Flawlessly assemble this invaluable data into a highly structured, impenetrable prompt explicitly engineered for the Master Integrator agent, mandating a rigidly typed Pydantic output.
    - Comprehensively refactor the overarching Orchestrator (`workflow.py`). Masterfully implement the highly advanced `asyncio.gather` logic fundamental to massively parallelizing the Phase 2 Coder graphs. Establish the critical, non-negotiable blocking await mechanism strictly required before seamlessly transitioning the aggregated payload into the sophisticated Phase 3 Integration workflow. Finally, meticulously ensure the flawless, sequential handoff to the ultimate Phase 4 QA sequence.

## Test Strategy
The incredibly complex testing strategy engineered for this comprehensive cycle fundamentally demands absolute, unyielding mathematical verification across all conceivable operational vectors.

### Unit Testing Approach
The highly rigorous unit testing protocol fundamentally prioritizes the absolute structural integrity of the newly forged Pydantic models and the flawless, deterministic execution behavior characteristic of the sophisticated conditional routing functions.

Within the heavily fortified `tests/test_state.py` module, exhaustive test matrices must be meticulously formulated to aggressively target the drastically modified `CommitteeState` and overarching `CycleState` configurations. We will systematically instantiate these complex models utilizing wildly disparate permutations of the newly minted fields (`is_refactoring`, `current_auditor_index`, `audit_attempt_count`). This crucial procedure mathematically verifies that the uncompromising Pydantic field validators flawlessly enforce all critical constraints (e.g., unequivocally confirming that `audit_attempt_count` can never structurally register as a negative integer). Furthermore, we will meticulously verify that the sophisticated backward compatibility properties engineered upon the `CycleState` perfectly and instantly map directly to the securely underlying `CommitteeState` fields, absolutely guaranteeing that interacting with these top-level properties flawlessly mutates the deeply nested execution state.

Subsequently, immensely powerful tests will be engineered specifically for the highly complex routing functions residing within `tests/nodes/test_routers.py`. We will meticulously construct elaborate dummy `CycleState` objects perfectly mirroring highly specific, deeply complex junctures within the advanced development cycle. For the incredibly critical `route_sandbox_evaluate` function, we will mathematically verify that a state explicitly carrying `sandbox_status="failed"` unconditionally outputs `"failed"`, while a fundamentally identical state carrying `is_refactoring=True` correctly and safely redirects to `"final_critic"`. For the vastly more complex `route_auditor` logic, we will aggressively simulate grueling, multi-layered rejection loops, meticulously ensuring that returning `"Reject"` flawlessly increments the highly sensitive attempt counter and reliably outputs `"reject"`. Conversely, we must verify that returning `"Approve"` successfully increments the internal auditor index and flawlessly transitions the execution flow toward `"next_auditor"` or the triumphant `"pass_all"` terminus. These intensely rigorous unit tests absolutely guarantee that the foundational LangGraph control flow logic is mathematically impenetrable before it is ever subjected to complex graph integration.

### Integration Testing Approach
The comprehensive integration testing protocol is explicitly designed to meticulously verify that the entirely rewired LangGraph state machines—specifically the `_create_coder_graph` and the newly minted `_create_integration_graph`—operate flawlessly as completely cohesive, perfectly synchronized execution engines. This incredibly demanding procedure involves rigorously executing the complex LangGraph traversals utilizing heavily mocked node behaviors. This critically important strategy seamlessly simulates incredibly complex real-world operational scenarios without ever incurring the devastating financial cost or unpredictable systemic side-effects inextricably linked to actual LLM code generation or highly volatile dynamic sandbox execution.

We will meticulously formulate immensely powerful integration tests permanently housed within `tests/test_coder_graph.py` and `tests/test_integration_graph.py`. A completely indispensable test scenario must flawlessly simulate an utterly perfect "Happy Path": seamlessly initializing the complex graph, flawlessly mocking the Coder to instantly synthesize perfect code, flawlessly mocking the restrictive Sandbox to immediately report success, flawlessly mocking three entirely sequential Auditors to unanimously approve the logic (mathematically verifying the `current_auditor_index` flawlessly increments exactly from 1 to 3), flawlessly transitioning the flow toward the dedicated Refactor node, flawlessly mocking the complex refactor pass (mathematically verifying the `is_refactoring` boolean instantly flips to `True`), easily passing the grueling final Sandbox evaluation, and conclusively securing absolute approval directly from the Final Critic. We will assert with absolute precision that the ultimate, terminal state successfully resting at the `END` node correctly and definitively reflects `status="completed"` and strictly holds `is_refactoring=True`.

Another fundamentally crucial integration test will aggressively simulate the incredibly stressful "Rejection Loop". We will ruthlessly mock a highly critical Auditor to aggressively reject the synthesized code multiple, consecutive times. We will mathematically verify that the complex graph logic reliably routes back directly to the Coder node following every single rejection. Specifically, we must assert with absolute precision that the `audit_attempt_count` flawlessly and accurately reflects the precise number of grueling loop iterations. Furthermore, we must actively simulate a devastating Sandbox failure occurring *during* the highly sensitive refactoring phase, meticulously ensuring the intelligent system correctly and safely routes the cascading failure back to the Coder for immediate remediation before ever attempting another final critique. Within the Integration graph tests, we will aggressively simulate highly complex 3-Way merge conflicts, strictly asserting that the intelligent `master_integrator_node` is correctly, decisively engaged and fundamentally resolves the systemic impasse. These extraordinarily demanding tests unequivocally guarantee that the revolutionary multi-phase routing logic is profoundly robust and mathematically handles both overwhelming success and catastrophic failure with absolute precision.
### D. Network Resilience & Retry Strategy
- **Mandate Exponential Backoff:** You MUST explicitly instruct the Coder that *all asynchronous HTTP requests executing against external APIs (e.g., `JulesClient._send_message`, OpenRouter completions, E2B sandbox interactions)* MUST be heavily fortified with an asynchronous retry decorator (such as `@retry` from the `tenacity` library).
- *Why:* The system interfaces with high-latency LLM providers. Without explicitly trapping `httpx.ReadTimeout`, `httpx.ConnectTimeout`, and `httpcore.ReadTimeout` exceptions and automatically retrying with exponential backoff, temporary network instability will catastrophically crash the entire LangGraph orchestration cycle.
- **Instruct the Coder to append the following explicit configuration to the system environment:**
  - `JULES_REQUEST_TIMEOUT=120.0` (Explicitly extending the underlying `httpx.AsyncClient` timeout to gracefully handle complex, slow cognitive queries like the Self-Critic).

### E. Integration Phase Circuit Breaker
- **Mandate Merge Retries Tracker:** The newly defined `IntegrationState` Pydantic model MUST explicitly include an `integration_attempt_count: int = Field(default=0)` tracker variable.
- *Why:* Complex 3-Way Diff resolutions may occasionally produce structurally invalid code, triggering a Sandbox rejection. To prevent an infinite feedback loop between the Sandbox and the Master Integrator, this specific counter MUST be incremented on every failed merge attempt, terminating the Integration Phase if it exceeds `NITPICK_MAX_INTEGRATION_RETRIES`.
